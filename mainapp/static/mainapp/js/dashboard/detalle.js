miapp.dashboard.detalle = function (argument) {

    miapp.dvmain.load("/static/mainapp/html/dashboard/detalle.html", function () {
        var dvTitle = $("#dvTitle");
        dvTitle.text('Dashboard');

        var cargar = function () {
            //cargar
        };

        cargar();
    });
};
